# KWEB 2026-1 프론트엔드 구제 과제 명세서

## 과제 개요
본 과제는 2026학년도 1학기 KWEB 프론트엔드 준회원을 대상으로 하는 정회원 승급 구제 과제입니다. 
제공된 React + Vite 템플릿과 Fake API(`json-server`)를 활용하여 핵심적인 프론트엔드 로직(상태 관리, API 연동, 조건부 렌더링, 리스트 렌더링)을 구현하는 것이 목표입니다.

## 과제 목표
- **React 컴포넌트 생명주기 및 상태 관리 이해**: `useState`, `useEffect` 등을 활용하여 데이터를 상태로 관리합니다.
- **REST API 통신**: `fetch` API를 사용하여 비동기 통신을 수행하고, 결과를 UI에 반영합니다.
- **배열 렌더링 및 조건부 렌더링**: `map()`을 이용한 리스트 렌더링과 조건에 따른 컴포넌트 마운트를 구현합니다.

## 구현 요구 사항 (TODO 항목)

제공된 템플릿 내부의 `TODO` 주석들을 찾아 아래 기능들을 완성해 주세요.
> **💡 Hint**: `src/api/todoApi.js`의 `getTodos` 함수와 `src/pages/TodoPage.jsx`의 `fetchTodos` 함수는 **정답 예시**로 미리 완성되어 있습니다. 이 패턴(`fetch` 사용법, `async/await`, 상태 업데이트 로직 등)을 적극적으로 참고하여 나머지 빈칸을 구현하세요.

### 1. API 통신 로직 완성 (`src/api/todoApi.js`)
- `POST /todos`: 새로운 할 일을 서버에 생성합니다. (기본 완료 상태: `false`)
- `PATCH /todos/:id`: 할 일의 완료 상태를 수정합니다.
- `DELETE /todos/:id`: 할 일을 서버에서 삭제합니다.

### 2. 컴포넌트 UI 로직 완성
- **목록 렌더링 (`src/components/TodoList.jsx`)**: 
  - 전달받은 `todos` 배열을 `map()` 함수로 순회하여 여러 개의 `TodoItem` 컴포넌트로 렌더링하세요.
  - 리액트 리스트 렌더링의 필수 항목인 `key` 속성을 잊지 마세요.
- **할 일 추가 (`src/components/TodoInput.jsx`)**: 
  - `createTodo` API를 호출하고 성공 시 입력창을 초기화한 뒤, 목록을 갱신하도록 구현하세요.
- **할 일 토글 및 삭제 (`src/components/TodoItem.jsx`)**: 
  - 체크박스 클릭 시 완료 상태를 변경하는 로직(`handleToggle`)과 삭제 버튼 클릭 시 처리하는 로직(`handleDelete`)을 완성하세요.
- **조건부 렌더링 (`src/App.jsx`)**: 
  - 상단 탭에서 선택된 `currentTab` 상태값에 따라 `TodoPage` 혹은 `ReviewPage` 중 하나만 화면에 보이도록 조건부 렌더링을 구현하세요.

### 3. 스터디 회고 작성 (`src/pages/ReviewPage.jsx`)
- 과제를 진행하며 느낀 점, 어려웠던 점, 새롭게 알게 된 점 등을 자유롭게 마크다운이나 HTML 태그를 활용하여 작성해 주세요.

## 실행 및 확인 방법
```bash
# 1. 패키지 설치
npm install

# 2. 서버 실행 (React 화면과 Fake API가 동시에 실행됩니다)
npm start
```
- 브라우저에서 `http://localhost:5173` 접속 후 정상 동작 여부를 확인합니다.

## 제출 방법
1. 원본 저장소를 본인의 GitHub 계정으로 Fork 합니다.
2. 로컬에 Clone 하여 모든 TODO 항목을 구현합니다.
3. 작업이 완료되면 원본 저장소로 **Pull Request**를 보냅니다.
4. PR 본문에는 **주요 설계 결정**, **막혔던 부분과 해결 과정**을 꼭 본인의 언어로 작성해 주세요.
